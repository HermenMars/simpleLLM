version: "3.9"

services:
  llm:
    build: .
    container_name: small-llm
    volumes:
      - ./src:/app/src
    ports:
      - "5000:5000"  # optional if you expose an API later
    stdin_open: true
    tty: true
